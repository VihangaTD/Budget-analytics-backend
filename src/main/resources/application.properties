spring.application.name=moneymanager

#Mysql configuration
spring.datasource.url=jdbc:mysql://localhost:3306/moneymanager
spring.datasource.username=root
spring.datasource.password=#Tharuka99

server.servlet.context-path=/api/v1.0

#jpa configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#Email Configuration
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=${BREVO_USERNAME}
spring.mail.password=${BREVO_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${BREVO_FROM_EMAIL}

#Jwt configuration
jwt.secret=1379f1dffec0f27d9ae517cd6cf109b764664dcff2d192a75228dda32d7b75f7
jwt.expiration=86400000

budget.analytics.frontend.url=${BUDGET_ANALYTICS_FRONTEND_URL}

app.activation.url=${BUDGET_ANALYTICS_BACKEND_URL}